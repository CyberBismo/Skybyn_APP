org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
org.gradle.parallel=false
org.gradle.caching=false
org.gradle.configureondemand=true
android.enableR8.fullMode=false
android.useFullClasspathForDexingTransform=true
android.enableUnitTestBinaryResources=false
android.testOptions.unitTests.all.enabled=false
org.gradle.daemon=true
org.gradle.workers.max=2
android.enableD8.desugaring=true
# Fix for Windows path resolution issues
org.gradle.unsafe.configuration-cache=false
org.gradle.configuration-cache=false
# Fix for Windows file locking issues - disable parallel and caching to prevent file locks
# These can be re-enabled if file locking issues are resolved

# Suppress Java compilation warnings (Note: compiler warnings are suppressed via build.gradle)
android.suppressUnsupportedCompileSdk=36
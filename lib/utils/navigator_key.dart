import 'package:flutter/material.dart';

/// Global navigator key — used for showing overlays and navigating without a BuildContext.
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
